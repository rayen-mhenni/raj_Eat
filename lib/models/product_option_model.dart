class ProductOption {

  final Map<String, bool> options;

  ProductOption({

    required this.options,
  });

  // Méthode pour convertir la classe en Map, compatible avec Firestore
  Map<String, dynamic> toMap() {
    return {


    };
  }
}